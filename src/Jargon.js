// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as Reactfire from "reactfire";
import * as Belt_Array from "../node_modules/rescript/lib/es6/belt_Array.js";
import * as Firestore from "firebase/firestore";

function Jargon(Props) {
  var id = Props.id;
  var commentsCollection = Firestore.collection(Reactfire.useFirestore(), "jargons/" + id + "/comments");
  var match = Reactfire.useFirestoreCollectionData(Firestore.query(commentsCollection, Firestore.orderBy("timestamp", "asc")), undefined);
  if (match.status === "loading") {
    return "loading";
  }
  var prompt = "Discussion on jargon " + id;
  return React.createElement("div", undefined, React.createElement("div", {
                  className: "dark:text-white"
                }, prompt), Belt_Array.map(match.data, (function (param) {
                    return React.createElement("div", {
                                className: "ml-4"
                              }, param.user + " | " + param.timestamp.toDate().toString(), React.createElement("br", undefined), param.comment);
                  })));
}

var make = Jargon;

export {
  make ,
}
/* react Not a pure module */
