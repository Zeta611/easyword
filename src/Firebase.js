// Generated by ReScript, PLEASE EDIT WITH CARE

import * as App from "firebase/app";
import * as Auth from "firebase/auth";
import * as FirebaseConfig from "./firebaseConfig";
import * as Analytics from "firebase/analytics";
import * as Firestore from "firebase/firestore";

var firebaseConfig = FirebaseConfig.firebaseConfig;

var DocSnapshot = {};

var QuerySnapshot = {};

function initializeServices(param) {
  var firebaseApp = App.initializeApp(firebaseConfig);
  var analytics = Analytics.getAnalytics(firebaseApp);
  var firestore = Firestore.getFirestore(firebaseApp);
  var auth = Auth.getAuth(firebaseApp);
  return {
          firebaseApp: firebaseApp,
          analytics: analytics,
          firestore: firestore,
          auth: auth
        };
}

var match = initializeServices(undefined);

var __x = Firestore.enableMultiTabIndexedDbPersistence(match.firestore);

__x.catch(function (err) {
      console.log(err);
      return Promise.resolve(undefined);
    });

var getFirebase = initializeServices;

export {
  firebaseConfig ,
  DocSnapshot ,
  QuerySnapshot ,
  initializeServices ,
  getFirebase ,
  
}
/* firebaseConfig Not a pure module */
